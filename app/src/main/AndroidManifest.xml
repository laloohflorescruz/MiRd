<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.crudcreativo.mird">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".SantoDomingoActivity"
            android:label="@string/title_activity_santo_domingo" />
        <activity
            android:name=".CibaoActivity"
            android:label="@string/title_activity_cibao" />
        <activity
            android:name=".EsteActivity"
            android:label="@string/title_activity_este" />
        <activity
            android:name=".SurActivity"
            android:label="@string/title_activity_sur" />
        <activity
            android:name=".MapaActivity"
            android:label="@string/title_activity_mapa" />
        <activity android:name=".Main2Activity"></activity>
    </application>

</manifest>